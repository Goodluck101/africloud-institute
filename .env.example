# Copy this file to .env and replace the placeholders.
# Never commit .env — it is listed in .gitignore.

SECRET_KEY=replace-with-a-long-random-string
FLASK_DEBUG=1
PORT=5000

# Gmail inbox for applications, contact, and corporate forms.
# Local sending uses Gmail SMTP as AfriCloud Institute.
# Render's free plan blocks SMTP, so production needs the Gmail web app
# relay in scripts/gmail_relay.gs. Emails still send FROM Gmail.
INBOX_EMAIL=africloudinstitute@gmail.com
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=africloudinstitute@gmail.com
SMTP_FROM=africloudinstitute@gmail.com
SMTP_PASSWORD=replace-with-gmail-app-password
GMAIL_WEBHOOK_URL=
GMAIL_WEBHOOK_SECRET=replace-with-a-long-random-string
